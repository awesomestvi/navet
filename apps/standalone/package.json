{
  "name": "@navet/standalone",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && node ../../scripts/remove-copied-doc-screenshots.mjs standalone",
    "postbuild": "node ../../scripts/check-built-module-syntax.mjs dist",
    "preview": "vite preview"
  }
}
