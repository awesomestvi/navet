name: Record Human Approval

on:
  issue_comment:
    types:
      - created
  pull_request_target:
    types:
      - opened
      - reopened
      - synchronize

permissions:
  contents: read

jobs:
  approve:
    name: Record maintainer approval for current head
    if: github.event_name == 'issue_comment' && github.event.issue.pull_request && startsWith(github.event.comment.body, '/approve-')
    runs-on: ubuntu-latest
    permissions:
      issues: write
      pull-requests: read
      statuses: write
    steps:
      - name: Record approval for reviewed commit
        uses: actions/github-script@v7
        env:
          HUMAN_APPROVER: ${{ vars.NAVET_HUMAN_APPROVER || vars.NAVET_PRODUCT_APPROVER }}
        with:
          script: |
            const permitted = new Set([context.repo.owner, process.env.HUMAN_APPROVER].filter(Boolean));
            if (!permitted.has(context.payload.comment.user.login)) {
              core.setFailed('Only the configured Navet approver can satisfy human gates.');
              return;
            }

            const [command, reviewedSha, ...extraArguments] = context.payload.comment.body.trim().split(/\s+/);
            const labels = {
              '/approve-foundation': 'review: foundation-approved',
              '/approve-security': 'review: security-approved',
            };
            const label = labels[command];
            if (!label) {
              core.setFailed(`Unknown approval command: ${command}`);
              return;
            }
            if (!reviewedSha || extraArguments.length > 0 || !/^[0-9a-f]{40}$/i.test(reviewedSha)) {
              core.setFailed(
                `Use ${command} <full-current-head-sha>. Copy the exact command from the PR review summary.`,
              );
              return;
            }

            const pull = await github.rest.pulls.get({
              owner: context.repo.owner,
              repo: context.repo.repo,
              pull_number: context.issue.number,
            });
            if (pull.data.head.sha.toLowerCase() !== reviewedSha.toLowerCase()) {
              core.setFailed(
                `The reviewed commit ${reviewedSha} is no longer the PR head (${pull.data.head.sha}). Review the current preview and approve again.`,
              );
              return;
            }
            const gate = command.replace('/approve-', '');

            await github.rest.repos.createCommitStatus({
              owner: context.repo.owner,
              repo: context.repo.repo,
              sha: reviewedSha,
              state: 'success',
              context: `navet/${gate}-approval`,
              description: `Approved by ${context.payload.comment.user.login}`.slice(0, 140),
              target_url: context.payload.comment.html_url,
            });

            await github.rest.issues.addLabels({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.issue.number,
              labels: [label],
            });

  invalidate:
    name: Invalidate approvals after a new commit
    if: github.event_name == 'pull_request_target' && github.event.action == 'synchronize'
    runs-on: ubuntu-latest
    permissions:
      issues: write
    steps:
      - name: Remove approvals for the previous head
        uses: actions/github-script@v7
        with:
          script: |
            for (const name of [
              'review: foundation-approved',
              'review: security-approved',
            ]) {
              try {
                await github.rest.issues.removeLabel({
                  owner: context.repo.owner,
                  repo: context.repo.repo,
                  issue_number: context.issue.number,
                  name,
                });
              } catch (error) {
                if (error.status === 404) continue;
                if (error.status === 403) {
                  core.warning(
                    'GitHub did not allow approval-label cleanup. SHA-bound statuses remain authoritative.',
                  );
                  break;
                }
                throw error;
              }
            }
