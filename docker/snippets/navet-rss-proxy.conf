include /etc/nginx/snippets/navet-rss-transport.conf;
# Same-origin RSS fetch proxy for production Docker / add-on nginx.
# njs authenticates callers; the local transport pins public DNS before fetching.
location = /__navet_rss_proxy__ {
  js_content navet_rss_proxy.handle;
}
