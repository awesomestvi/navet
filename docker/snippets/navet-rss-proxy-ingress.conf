include /etc/nginx/snippets/navet-rss-transport.conf;
# Only the supervisor-restricted Ingress server may trust forwarded user identity.
location = /__navet_rss_proxy__ {
  js_content navet_rss_proxy.handleIngress;
}
