# Frontend audit — 22 September 2026

This is the first pass on `feature/performance-accessibility-audit`. It covers the four public entrypoints, the shared app shell, build output, and targeted security and accessibility patterns. It is not a claim that every dashboard state, story, or documentation page has been audited.

## Verified changes

| Area | Change | Verification |
| --- | --- | --- |
| Distribution size | The standalone, demo, website, and Storybook builds no longer ship copies of docs-only how-to screenshots. The docs build retains them. | Built output fell from 17,848 to 14,464 KiB (standalone), 17,952 to 14,184 KiB (demo), 18,960 to 15,192 KiB (website), and 35,312 to 31,544 KiB (Storybook). |
| Standalone install cache | The service worker now precaches CSS imported by its static entry graph. Lazy map-widget CSS remains in the distribution and enters the immutable runtime cache when requested, like its lazy JavaScript. | The generated service worker kept the main stylesheet and reduced its install list from 31 to 30 files. Workbox reported 1,512.09 to 1,431.07 KiB of install payload; the removed map stylesheet is 82,869 raw bytes. A regression test first caught the lazy CSS in the shell graph, then passed after the fix. An installed local preview served the app and both stylesheets after an offline reload, then showed its session-recovery error because the static preview has no auth backend; authenticated offline behavior remains unverified. The map feature was already unavailable on a first offline visit because its lazy JavaScript was not precached. |
| Non-app install assets | Website, demo, docs, and Storybook builds now remove copied PWA install icons and the unlinked standalone web manifest after checking their generated resources for references. The standalone app keeps its linked manifest and icons; every public surface keeps its favicon and Apple touch icon. | Each of the four public distributions removes 166,201 bytes, totaling 664,804 shipped bytes. All four normal builds and the website audit passed. The website still reports 19 initial assets and 410.0 KiB gzip; this is a distribution-size saving, not a first-load download reduction. |
| Unlinked social cards | Docs and Storybook no longer copy the shared social-card JPEG into their output; neither site references that local URL in its pages or metadata. Website and demo retain the image used by their social metadata, and standalone retains its own linked copy. | The guarded public-asset prune checks generated references and exact source bytes before removal. Docs fell from 9,451,527 to 9,343,706 bytes and Storybook from 23,815,663 to 23,707,842 bytes (107,821 fewer each). All four affected builds, docs and Storybook CSP checks, Storybook standards, and the website audit passed. These unlinked copies were not part of initial page transfer. |
| Shared font size | Standalone, demo, website, docs, and Storybook builds now remove copied public Inter files after verifying that each build emitted matching font assets and no output references the public URLs. The font license remains. | Each distribution removes 133,324 duplicate font bytes. The docs distribution fell from 11,416 to 11,312 KiB, the website from 12,412 to 12,232 KiB, and Storybook from 28,608 to 28,428 KiB on disk. All five builds and the standalone bundle budget passed; a local docs preview reported Inter available with one main landmark. |
| Source-only public files | Each production build now drops the copied public-asset README, boot-script TypeScript declaration, and boot-script test after confirming each copy matches its repository source. The source files remain available for contributors and tests. | All five normal builds passed and omitted the three files, removing 8,217 bytes per distribution (41,085 bytes across the five). A generated-HTML check found no local link to a removed file. The focused boot-script tests passed; standalone bundle and website initial-asset budgets were unchanged. This is a distribution-size and source-exposure cleanup, not an initial page-transfer saving. |
| Storybook internal asset size | The Storybook build removes four root Nunito fonts and a root favicon wrapper only after confirming byte-identical copies in `sb-common-assets` and checking generated text assets for root references. | The normal build removed 199,099 duplicate bytes and passed its CSP gate. The local Fonts docs story rendered with no browser errors; its referenced shared font URL returned 200 while the removed root URL returned 404. |
| Docs distribution size | The docs build drops copied dashboard wallpapers after checking that its generated pages and scripts have no wallpaper references. Other distributions retain the wallpapers for their app and Storybook flows. | The docs build passed and removed 33 unreferenced files totaling 369,825 bytes (about 361 KiB). |
| Docs SVG duplication | The docs build now removes a generated SVG only when another SVG has identical bytes and is the sole referenced copy. The three redundant Astro branding diagrams are no longer shipped. | A fresh docs build removed 333,943 bytes across three files, then a second prune pass removed zero. The 79-page CSP check passed, and a local preview decoded the retained card anatomy, logo system, and color architecture diagrams at their expected intrinsic sizes. These were unreferenced copies, so no first-load transfer saving is claimed. |
| Provider logo size | Sign-in and provider settings reuse a 112 px Homey WebP instead of a 171,137-byte SVG wrapper around a PNG. The smaller marketing asset stays in place. | Standalone, demo, Home Assistant panel, and Storybook builds emit the same 4,542-byte WebP with no Homey SVG. The demo's System settings loaded it at 112 × 112 natural size; focused login and settings tests passed. Standalone eager JavaScript stayed at 752.2 KiB, and the website audit stayed at 409.9 KiB gzip. |
| Shared stylesheet size | Marketing-only Tailwind utilities are generated by the website stylesheet rather than every app entrypoint. | Production demo and standalone CSS each fell by 19,490 bytes (568,824 to 549,334 and 568,812 to 549,322 bytes). The website CSS remained byte-identical at 618,078 bytes. Website, Storybook, and Home Assistant panel builds passed; all eight demo sections retained one main landmark and no horizontal overflow in a local production preview. |
| Keyboard access | A shared skip link now reaches the main content in the demo and website. The demo adds a main landmark where needed, including Household loading and content states; the mobile Settings list now has one before a detail is opened. | Local browser: keyboard activation focused the target. A 390 px production preview exposed one main landmark on all eight demo routes and on both mobile Settings list and detail views. |
| Demo lazy-section loading | The Household and Settings route fallback now exposes a main landmark while their section modules load; those sections retain ownership of the landmark after loading. | A rebuilt local production preview showed one visible main landmark and `aria-busy="true"` during Household loading, then one visible main and no busy state after it loaded. Settings also rendered one visible main; demo tests, typecheck, and build passed. |
| Keyboard scrolling | Energy's usage list and the shared status summary can receive keyboard focus when their content scrolls on a phone. The summary adds no tab stop at desktop widths. | At 390 px, Arrow Down moved the Energy list 40 px and Arrow Right moved the Climate summary 40 px. A WCAG 2.0/2.1 A/AA axe scan found no violations or horizontal page overflow on any of the eight demo routes after the change. |
| Accessible control names | The compact Security alarm controls are a named group, the keyboard-scrollable activity list is a named region, and the shared media slider leaves its accessible name on the actual slider thumb rather than its generic wrapper. | A 390 px production demo preview exposed one Alarm controls group, one Recent activity region, eight Seek sliders, and one Volume slider. Axe's previously unresolved `aria-prohibited-attr` results fell to zero on Home, Security, and Media; 43 focused Security tests, typecheck, and the demo build passed. |
| Dialog title IDs | Modal and fullscreen screens with a visible `CardDialogHeader` now let that header supply the Radix title instead of also rendering the base dialog's hidden title. The shared header uses a semantic `h2`, preserving both the dialog name and its visible heading across Add Card, provider, security, chores, settings, and device dialogs. | The mobile Add Card Storybook view exposed two elements with the same ARIA title ID before the change. After rebuilding, it had one matching title ID, a visible heading, no page overflow or browser errors, and no confirmed axe WCAG A/AA violations; Radix focus guards and gradient contrast remained inconclusive. Focused dialog tests, Tier 3 (525 files, 3,579 tests), typecheck, Storybook, standalone, demo, and Home Assistant panel builds, UI standards checks, and bundle budget passed. No bundle-size saving is claimed. |
| Authenticated keyboard access | The dashboard now exposes the shared skip link, with a focus target immediately after its header and before section content. | Dashboard page and layout tests passed; typecheck and focused Biome passed. |
| Dashboard editor names and focus | The Home section title editor now has a translated accessible name and a visible keyboard focus ring in each theme. The shared skip link also uses the active app language instead of fixed English. | A regression test could not find the unnamed editor before the change and passes after it. The focused dashboard tests, Storybook browser interaction, all 13 locale checks, typecheck, and standalone, demo, website, and Storybook builds passed. The rebuilt Storybook preview showed the named editor and a passing interaction in light mode; its accessibility add-on reported no confirmed violations in light and dark modes, with one gradient contrast case inconclusive. |
| Authenticated dashboard landmarks | Home and the feature sections now expose a main landmark through the section router. Tasks and Settings keep the main landmarks owned by their section components, avoiding nested landmarks. | Focused router tests cover Home, Climate, Energy, Tasks, and Household with exactly one main landmark; 22 router tests and typecheck passed. |
| Authenticated loading landmarks | The initial authenticated loading screen and the dashboard's connecting state now expose a main landmark with a busy state; the connecting state also keeps the skip link available. | Dashboard page and app tests passed (36 tests), along with typecheck, standalone build and bundle budget, and Home Assistant panel build. |
| Session bootstrap landmark | The app gate's full-screen loading and session-recovery surface now owns a main landmark. It reports busy while startup is pending and clears that state when retry or return-to-login actions appear. | A focused regression test covered both states among 27 passing App tests. After a service worker controlled offline reload, the local standalone preview showed the session-recovery surface with exactly one main landmark, `aria-busy="false"`, two stylesheets, no horizontal overflow, and no page errors. Axe found no confirmed WCAG A/AA violations; two contrast cases remained inconclusive. Typecheck, standalone, and Home Assistant panel builds passed. |
| Authenticated error landmarks | The full-screen error surface now owns the main landmark and receives focus. The still-mounted dashboard or Homey selection screen is inert and hidden from assistive technology until the error clears; the Home Assistant panel uses the same background handling. | An app regression test verified one exposed main, focus on the error, an inert dashboard, and restoration after clearing the error. The 37 focused app and dashboard tests, typecheck, standalone and panel builds, bundle budget, and Tier 3 regression tier (523 files, 3,565 tests) passed. |
| Standalone sign-in | The production standalone preview exposes one main landmark and one H1 on provider selection and the Home Assistant, Homey, and openHAB connection forms. | A 390 px axe WCAG 2.0/2.1 A/AA scan found no confirmed violations on all four states. The Home Assistant option opened by keyboard at 320, 390, 768, and 1280 px with no horizontal overflow. Contrast checks on translucent backgrounds remained inconclusive, and authenticated provider sign-in was not exercised in the static preview. |
| Authenticated dashboard updates | The section router now observes chore workspace data only while showing a specific Home room, where it is used for the room summary and chore cards. | A focused test updates the chore workspace while Climate is active and confirms the dashboard layout does not rerender. The other 16 router tests and typecheck passed. |
| Dashboard shell updates | The navigation chrome now owns the header controller's search, avatar, and notification subscriptions. Header-only updates no longer recalculate the surrounding wallpaper and layout surface; the sidebar and header still share one controller. | A regression test first observed one extra dashboard-surface calculation when header text changed, then zero after the extraction. All 14 layout tests, Tier 2 (159 tests), typecheck, Biome, demo and standalone builds, and standalone bundle budget passed. A 390 px production demo preview showed one main landmark and no overflow or browser errors on Home and Settings. This reduces render work; no bundle-size gain is claimed. |
| Closed notification panels | Desktop and mobile headers now mount the notification panel only while it is open. The always-visible unread badge keeps its own subscription, and the desktop bell exposes its expanded state. | In local production demo previews, desktop and 390 px phone notification panels opened and closed with the expected accessible state and content. The six focused header/controller tests, Tier 2 (159 tests), typecheck, standalone, demo, and Home Assistant panel builds, focused Biome, and bundle budget passed. The demo entry changed from 814,220 to 814,263 bytes and standalone eager JavaScript stayed at 752.0 KiB, so this is a subscription and render-work improvement, not a size saving. |
| Dashboard card icon reuse | Light, switch, fan, humidifier, and sensor cards now share the saved-icon decision while keeping their existing per-card fallback icons and storage behavior. The helper resolves a saved Lucide name once and retains emoji and unknown-icon fallback behavior. | Focused tests covered saved aliases, case-insensitive names, emoji, unknown names, and the light display hook. Tier 2 (159 tests), typecheck, standalone, demo, and Home Assistant panel builds, and bundle budget passed. The demo entry decreased from 814,263 to 814,112 bytes; standalone eager JavaScript remained 752.0 KiB. |
| Home edit rendering | Home edit mode now mounts only the editor's card collection path. The normal presentation collection and viewport work mounts only in view mode, and the editor reuses the shared card-map builder instead of repeating its loop. | A mode-toggle regression confirmed the normal collection hook runs once in view mode, does not run when entering edit mode, and resumes when returning. The focused Home and shared-layout tests (11 tests), Tier 2 (159 tests), typecheck, standalone, demo, and Home Assistant panel builds, Storybook standards, and bundle budget passed. Eager JavaScript remained 752.1 KiB and authenticated transition JavaScript 139.8 KiB, so this is a render-work and code-reuse improvement without a measured bundle-size saving. |
| Home card placement reuse | View and edit modes now use one Home topology builder for section assignments, missing-card filtering, flow cards, and the editor's selected-card count. The builder collects flow cards during its existing traversal instead of filtering the selected IDs again. | Focused tests check that the valid calendar card stays in its section, the missing saved card stays out of the editor count, and assigned cards remain visible in flow mode. The 13 focused Home tests, Tier 2 (159 tests), typecheck, standalone, demo, and Home Assistant panel builds, and bundle budget passed. Eager and authenticated transition JavaScript remained at 752.1 KiB and 139.8 KiB; no size saving is claimed. |
| Home Assistant UPS widget updates | The UPS widget now reads normalized device collections and source devices only for providers that use them. Home Assistant keeps its own UPS data pipeline without subscribing to those unused provider collections, avoiding a widget rerender when unrelated normalized devices update. | A render-count regression failed at two renders before the selector change and passed at one afterward. Another test confirmed Homey source-device name changes still update the widget. Four focused UPS tests, Tier 2 (159 tests), typecheck, standalone, demo, and Home Assistant panel builds, and the bundle budget passed. Eager JavaScript remained 752.0 KiB; this is a render-work improvement. |
| Generic entity alias lookup | Generic entity cards now build one first-match alias index per provider view snapshot. Cards using saved external IDs reuse the index on unrelated store updates while retaining provider order and picking up new views when the snapshot changes. | A focused test covers duplicate external IDs across providers and a changed view snapshot; 26 related tests, Tier 2 (159 tests), typecheck, standalone and Home Assistant panel builds, Biome, and the standalone bundle budget passed. The lookup is linear once per view snapshot instead of linear for each card on each store notification; no bundle-size gain is claimed. |
| Dashboard grid fit | The shared card-grid hook now applies the same responsive gap that it uses to calculate minimum grid width. The Media dashboard uses that shared gap as well, avoiding a mismatch between layout and fit calculations on narrow screens. | A 390 px Energy Storybook preview had 2 px of page overflow before the change and none after; its Security, Media, and household story neighbors also had no page overflow, browser errors, or confirmed axe WCAG A/AA violations. Gradient contrast remains inconclusive. The 85 focused grid, Energy, and Media tests, typecheck, standalone, demo, Storybook, and Home Assistant panel builds, bundle budget, and 12-scenario visual review passed. |
| Room grouping updates | The section router now observes room workspace changes while Home is active or in kiosk mode. Ordinary non-Home sections no longer rerender on room organization changes; the kiosk control center still has current groups from every section. | Focused tests verified no Climate layout rerender, fresh groups on returning Home, and a live kiosk group update outside Home. All 19 router tests, Tier 2 (159 tests), typecheck, focused Biome, standalone build, and bundle budget passed (752.0 KiB eager and 139.8 KiB authenticated transition). This is a render-work change, with no bundle-size reduction claimed. |
| Provider room-management updates | The dashboard section router now observes manageable provider rooms only on Home, where it supplies room ordering and management controls. Other sections no longer recompute the router for these changes; Home still receives live room references. A dead empty effect in the same router was removed. | A focused regression first measured one extra Climate layout render after a provider room update, then zero after the selector change. The Home control test verified the new provider room reference reaches the layout. The 27 focused router tests, Tier 2 (159 tests), typecheck, standalone and Home Assistant panel builds, and bundle budget passed (752.0 KiB eager and 139.8 KiB authenticated transition). This is a render-work change; no bundle-size saving is claimed. |
| Disabled device collections | The two device collection hooks now reuse one room placement lookup and ignore room and override store updates while disabled. They resume updates when enabled, keeping inactive dashboard sections from recomputing room placement. | The focused hook test changes provider rooms, room workspace, and entity room overrides while both hooks are disabled, then verifies that an override update rerenders after enabling. All 15 device hook tests and typecheck passed. |
| Provider feature subscriptions | Calendar and weather collection hooks query an explicit provider's feature matrix. That matrix no longer subscribes to the selected-provider list, so changing the selection does not rerender these fixed-provider consumers. Aggregate feature queries still observe the selection. | Focused tests confirmed zero additional renders for an explicit provider and an update for the aggregate query after changing providers; typecheck and focused Biome passed. |
| Fixed-provider query subscriptions | Entity, connection, temperature, calendar, and weather query hooks share a selector that resolves explicit provider IDs without subscribing to current-provider changes. Queries without an explicit provider still follow the current provider. | A render-count regression test verified both cases. The 25 focused hook tests, Tier 2 (159 tests), Tier 3 (524 files, 3,567 tests), typecheck, standalone and Home Assistant panel production builds, and standalone bundle budget passed (752.0 KiB eager, 139.8 KiB authenticated transition). This reduces unrelated dashboard updates; no bundle-size reduction is claimed. |
| Stored icon lookup | The dashboard's icon resolver now builds one case-insensitive Lucide lookup containing canonical names and components, instead of building two large maps and intermediate arrays from the full registry. | Focused tests cover built-in aliases, stored Lucide names, case variants, and unknown names; the light-card display tests, typecheck, Biome, standalone build, and bundle budget passed. Eager JavaScript remains 752.0 KiB; the full Lucide namespace is still a tagged bundle-size seam. |
| RSS article links | Small, medium, and large RSS cards now share one link wrapper. It validates the rendered `href` as well as the click action, so an invalid stored or sample item cannot expose a dangerous target through browser link actions. | A regression test covers valid and `javascript:` URLs in all three sizes. The four focused RSS tests, typecheck, Biome, standalone and demo builds, and standalone bundle budget passed. No measurable eager-bundle reduction is claimed. |
| Relative image URL safety | The shared Home Assistant resource path check now rejects backslashes, control characters, and decoded leading double slashes in the path before any root-relative route, including the proxy prefix. On HTTP(S) pages, a value beginning with a slash followed by a backslash or line break can resolve to another host rather than a local image under the [URL Standard](https://url.spec.whatwg.org/). Query values remain intact for signed resources. | A URL-helper regression failed before the fix; resolver tests confirm malformed direct and proxy-prefixed paths become unavailable instead of exposing an image URL. Existing signed proxy paths remain green. The 18 focused tests, Tier 3 (526 files, 3,590 tests), typecheck, Biome, standalone, demo, and Home Assistant panel builds, and bundle budget passed. |
| Calendar and weather mapping reuse | Calendar and weather device hooks now share their identical entity name and room fallback functions, removing duplicate policy from the two hooks. | Their seven focused hook tests, typecheck, demo and standalone builds, and standalone bundle budget passed. The demo entry script remained 1,022,403 bytes before and after; this is a maintenance improvement with no measured demo entry-size gain. |
| Disabled provider collections | The shared calendar and weather collection lifecycle now skips writing a fresh empty state when a disabled collection is already empty, avoiding a second render on mount. Disabling a previously loaded collection still clears its data. | A render-count test failed at two renders before the change and passed at one afterward; a second test verified clearing loaded data. The 11 focused lifecycle, calendar, and weather tests, typecheck, standalone build, and bundle budget passed. |
| Accent text contrast | Shared primary actions, marketing links, dialog actions, user initials, and the edit navigation action now choose a black or white foreground from accent luminance. | The initial homepage scan found white on orange at 2.8:1. A rebuilt homepage scan found no WCAG A/AA violations. Button, dialog, and menu Storybook states also had no automated violations; the edit navigation story rendered black text on orange in browser. A focused test verifies at least 4.5:1 for all preset accents and light and dark custom examples, matching [WCAG 2.2 contrast minimum](https://www.w3.org/TR/wcag/#contrast-minimum). |
| Energy source link contrast | The authenticated Energy dashboard uses its surface text color for the Home Assistant Energy link in light mode while keeping the accent border. Dark themes retain their accent text. | The default orange text measured 2.80:1 against white; the light surface's slate-950 text measured 20.17:1 against white. The focused Energy test, typecheck, Biome, standalone build, and bundle budget passed. |
| Navigation semantics | The website's three main demo and install calls to action now share an anchor component styled with Navet's button tokens. | Local website: each call to action exposed a link with the expected destination and 40 px height. At 390 px wide, the layout had no horizontal overflow. |
| Shared subscriptions | Components using the same media query now share one browser change listener. | Focused hook test confirmed two consumers both update while the query has one active listener, and the listener is removed after the last consumer unmounts. |
| Demo initial load | The demo now loads seven non-home section views on demand, following the app dashboard's existing lazy-section pattern. Energy and Household sample setup stays with those sections. Root section URLs also restore the selected section after refresh. | The entry script fell from 1,349 to 1,087 KiB (about 19%). A local production preview rendered all seven sections through navigation and direct URLs without console errors; completing a sample chore still updated Household totals. |
| Demo dashboard imports | The demo imports the three dashboard modules it uses directly, avoiding eager evaluation of the authenticated dashboard controller and section router through the dashboard barrel. | A sourcemap build's entry fell from 1,087,959 to 1,055,118 bytes (32,841 bytes, about 3%). Neither controller nor router appears in the resulting entry sourcemap. Demo tests, typecheck, and local production navigation through Home, Energy, and Household passed. |
| Demo Add Card loading | The Add Card dialog is now loaded only when the demo enters edit mode or opens the dialog, with a loading state if its chunk has not arrived yet. | The demo entry fell from 1,058,375 to 1,022,403 bytes (35,972 bytes). A local production preview opened the dialog, selected Quick Note, added it to Home, and enabled Undo. The standalone build and bundle budget remained green. |
| Demo Security loading | The Security section's wrapper and model builder now load with that section instead of the Home entry. Its sample device data remains available for the existing demo flow. | The demo entry fell from 1,022,403 to 1,001,406 bytes (20,997 bytes); 56 focused demo and Security tests, typecheck, demo and standalone builds, and the standalone bundle budget passed. A production preview rendered Security through both its direct URL and Home navigation, with no browser errors. |
| Demo vacuum loading | The Home sample grid now defers the vacuum card code until that lower card approaches the viewport, reusing the dashboard's visibility hook and retaining the card's grid footprint while it loads. | The demo entry fell from 1,001,406 to 945,460 bytes (55,946 bytes, about 5.6%); the deferred vacuum chunk is 57,942 bytes and is absent from the initial browser asset list. In a local production preview it loaded after scrolling, rendered the card, kept the 4,602 px page height, and produced no browser errors or horizontal overflow at a 331 px viewport. Typecheck, the focused demo test, Tier 2 (159 tests), demo and standalone builds, and standalone bundle budget passed. |
| Demo Weather loading | Home defers its lower Weather card until it approaches view, and the Outside room loads the same card immediately on navigation. Both sample instances share one wrapper and preserve their grid footprint while loading. | The demo entry fell from 945,460 to 886,043 bytes (59,417 bytes, about 6.3%); Weather moved to a 67,235-byte chunk. At a 390 px viewport, the initial Home view made no Weather request; scrolling loaded and rendered the card without changing the 4,602 px page height, overflow, or browser errors. Outside also rendered its Weather card after room navigation without errors. The three focused demo test files, typecheck, Biome, demo and standalone builds, and standalone bundle budget passed. |
| Demo Calendar loading | Home's lower Calendar card now loads as it approaches the viewport, while Kitchen requests it when the room opens. Calendar, Weather, and Vacuum share one visibility and loading wrapper that preserves each card's grid footprint. | The demo entry fell from 886,659 to 860,668 bytes (25,991 bytes, about 2.9%); Calendar moved to a 26,955-byte chunk. A 390 px production preview made no initial Calendar request, then rendered its events on scroll with the 4,602 px page height unchanged. Kitchen rendered the same events immediately; Weather and Vacuum still loaded on scroll without errors or height changes. The focused demo test, typecheck, and demo build passed. |
| Demo Energy Now loading | Home's lower Energy Now card now loads as it approaches the viewport through the same card slot used by Calendar, Weather, and Vacuum. | The demo entry fell from 860,668 to 843,658 bytes (17,010 bytes); the card chunk is 15,636 bytes and absent from initial requests. A 390 px production preview loaded it on scroll with the 4,602 px page height unchanged, one main landmark, no overflow, and no browser errors. The direct Energy route still rendered. The focused demo test, typecheck, and demo build passed. |
| Deferred demo card keyboard access | Vacuum, Weather, and Calendar now expose a named keyboard stop while their below-view chunks load. If focus remains there, it moves to the first card control when loading completes; if focus moves away, the card does not take it back. Energy Now remains an information-only deferred card. | A 390 px production preview previously skipped Vacuum during Tab navigation. After the change, Tab reached its loading stop and then focused Start Cleaning. Delayed Weather and Calendar chunks transferred focus to the Stockholm control and first event; a delayed Vacuum load did not steal focus after Tab moved to the next widget. Home retained its 4,602 px height, one main landmark, no overflow, and no browser errors. Axe reported no confirmed WCAG A/AA violations; 91 composited contrast cases remained inconclusive. |
| Demo Switch loading | Home's three lower Switch cards defer through one shared wrapper with named keyboard stops. Kitchen and other room views request the same module when opened. | The demo entry fell from 844,943 to 814,184 bytes (30,759 bytes); Switch moved to a 26,734-byte chunk. At 390 px, the initial Home view made no Switch request. Tab reached Desk power while its chunk loaded, then the card control and Toggle Desk power. Clicking the toggle changed its visual state. Home stayed 4,602 px high with one main landmark, no overflow, and no page errors. Kitchen's Espresso and Basement's Power appeared after navigation. Axe found no confirmed WCAG A/AA violations; composited contrast remained inconclusive. |
| Demo camera cost | The camera card is now loaded when the user opens the Outside room. Its card slot retains its footprint while loading. | The 134 KiB camera chunk is no longer in the demo home page's modulepreload list. In a local production preview, Outside showed a loading state, then the camera image and controls without console errors. |
| Demo camera assets | Recompressed the sample camera's WebP and AVIF at the existing 2048 × 1536 resolution. | The WebP fell from 1,268,170 to 601,762 bytes; the AVIF from 629,152 to 423,037 bytes. Both decoded at full resolution and were visually reviewed. Local production preview loaded the new AVIF in Outside at its full natural dimensions. |
| Docs image layout | The docs build now adds actual intrinsic dimensions to local images across all generated pages. | The build added dimensions to 299 images on 78 pages. A generated-HTML check found all 302 local images have width, height, and alt attributes; a local browser preview rendered the Security screenshots at responsive widths without errors. |
| Docs screenshot loading | The same build pass now gives local guide images lazy loading and asynchronous decoding, matching Astro's generated image behavior. | All 302 local images across 79 built HTML pages have dimensions, alt text, `loading="lazy"`, and `decoding="async"`. At 390 px, the Add Cards guide requested three screenshots initially instead of four, deferring its 50,604-byte final image. That image decoded at 884 × 893 and rendered at 358 px wide when scrolled into view. The docs home and Add Cards guide had no automated WCAG A/AA violations or page overflow at 390 px. |
| Layout stability | The website hero background image declares its intrinsic dimensions. | Local website preview rendered the image with `width="1672"` and `height="941"`; no desktop horizontal overflow. |
| Marketing image size | The hero uses the existing WebP as its fallback and AVIF for capable browsers, so the 1,974,753-byte source PNG stays available for social-card generation without entering website or Storybook bundles. | Website output fell by 1,974,838 bytes and Storybook output by 1,974,441 bytes. Local previews at 390 and 1280 px decoded the 1672 × 941 AVIF, kept the WebP fallback, and had no page errors or horizontal overflow. |
| Marketing screenshot size | The landscape, portrait tablet, and phone dashboard previews now use their existing WebP files as fallbacks and RSS thumbnails, while retaining AVIF sources for capable browsers. This removes three redundant JPEGs from the website and Storybook distributions. | Both builds fell by 668 KiB on disk (website 12,232 to 11,564 KiB; Storybook 28,428 to 27,760 KiB). The website build, Storybook build, website audit, typecheck, and focused marketing data tests passed. A local production preview decoded the landscape AVIF at 1448 × 1012 and rendered it in the wall-panel frame. |
| Unused marketing media | The website preview data no longer exports an unused media-source table or an unselected camera card. This removes their AVIF and WebP imports without changing the cards displayed by the website. | The rebuilt website fell from 11,281,198 to 10,205,435 bytes on disk (1,075,763 fewer); Storybook fell from 24,294,763 to 24,240,633 bytes (54,130 fewer) while retaining camera samples used by stories. The website audit still found 19 initial assets. In a local production preview, the Home page showed its card preview with one main landmark, 16 loaded images, no failed images, and no horizontal overflow; the Roadmap route also rendered. No first-load transfer saving is claimed. |
| Media story artwork | The Media Card and Media Dashboard stories reuse the 1500 × 1500 AVIF artwork already emitted for other stories instead of importing a separate JPEG of the same artwork. | The Storybook output fell from 24,240,633 to 23,823,880 bytes on disk (416,753 fewer), and the JPEG is absent from the rebuilt output. A local production preview of the Media Card story decoded the AVIF at 1500 × 1500; the Storybook build and standards check passed. This reduces distribution size and the affected stories' image transfer, not the dashboard's initial transfer. |
| Marketing skip link integration | The shared skip link now accepts its label from its caller. Dashboard and demo callers translate it through their app language provider; the English website shell renders without that provider. | Before the change, the rebuilt website rendered a blank page with `useI18n must be used within I18nProvider`; a shell test reproduced the missing-provider crash. After the change, the test and local Home and Roadmap previews rendered with a working skip link. The rebuilt demo showed the link and Home dashboard. Standalone builds with and without the embedded demo passed emitted-module checks and bundle budgets; Tier 3 passed 526 files and 3,590 tests. |
| Build compatibility | Demo, website, Home Assistant panel, and Storybook Vite configs now use `import.meta.dirname`; the panel config also includes the extension on its local chunking import. | Native-loader demo build and normal website, panel, and Storybook builds were used to verify config loading. |
| Test configuration reuse | Unit and Storybook projects now inherit one shared Vitest setup, aliases, and build definitions. The unit file filter stays only in the unit project, and the root config uses `import.meta.dirname` for Vite's native loader. | A focused test passed under `--configLoader native` without the earlier loader or Storybook include warnings. A real Skip Link Storybook browser story, Tier 2 (159 tests), Tier 3 (524 files, 3,569 tests), and typecheck passed. |
| Demo script policy | The demo now uses the website's script CSP without `unsafe-inline`. Its build checks the final HTML for inline scripts before writing the policy. | Demo and Storybook production builds passed. A local Chromium preview with the generated demo CSP applied as a response header rendered Home, Energy, and Settings with no CSP violations or application errors. |
| Docs script policy | The docs build now hashes its 13 distinct inline boot scripts and writes those hashes into the generated CSP instead of allowing every inline script. A post-build gate checks every generated HTML page against that policy. | Across 79 generated pages, 1,006 script instances matched the 13 hashes. A local Chromium preview under the generated CSP rendered the homepage, dashboard guide, and changelog without CSP violations or page errors; the dashboard guide's search dialog opened. Deployment verification remains necessary. |
| Storybook preview policy | Storybook's generated manager CSP now allows same-origin frames, so its own preview iframe can load while cross-origin frames remain restricted to the existing YouTube origin. | On 23 September, the live Colors docs story showed a blocked preview. Its manager response had `frame-src` limited to YouTube while the iframe used `/iframe.html`; the iframe response allowed `X-Frame-Options: SAMEORIGIN`. The rebuilt `_headers` adds `'self'` to `frame-src`. A local production preview served with that CSP rendered the Colors docs content inside its frame without browser errors. Deployment verification remains necessary. |
| Storybook boot script policy | The Storybook build now hashes its two manager and three preview boot scripts separately. The preview route allows the same-origin manager to embed it, and a post-build gate checks both policies. | The production build passed. Under the generated CSP in local Chromium, the Colors docs story and the Button default story rendered inside the preview frame without CSP violations or page errors. Deployment verification remains necessary. |
| Website structured-data policy | The website build now hashes the generated JSON-LD separately for the homepage and roadmap and adds each hash to that route's script CSP. The OAuth callback retains its existing policy. | On 23 September, the live homepage reported a CSP violation for its inline JSON-LD. The rebuilt website audit checked both exact content hashes; Chromium rendered both routes under their generated headers with one main landmark, no CSP violation, and no page error. Deployment verification remains necessary. |

The live home, demo, docs, and Storybook roots returned HTTP 200. The docs and Storybook already expose skip links. All four sites send HSTS, a content security policy, a referrer policy, a permissions policy, and `X-Content-Type-Options: nosniff`. The production demo rendered its dashboard sections; docs and Storybook rendered their navigation and primary content. The docs build produced 78 pages and kept its how-to screenshots. These checks are a route and markup sample, not a full accessibility certification.

On 23 September, a fresh browser sample found one main landmark on the live website, docs homepage, and Storybook shell, but none on the live demo Home view; the deployed demo also had no skip link. The branch's local production preview has the main landmark and skip link, so deployment verification remains necessary. All four sampled roots declared `lang="en"` and had no page-level horizontal overflow at the browser's normal viewport. The visible demo greeting is duplicated in the DOM for responsive headers, with only one heading exposed in the accessibility tree.

A later live sample of the website and docs dashboard guide found one main landmark on each, one guide H1, semantic table headers, and no broken local anchors on that guide. Their sampled new-tab links declared `rel="noreferrer"`. The website's deployed Explore the demo action is still exposed as a button; the branch's local navigation fix has not yet been deployed.

A cold 390 × 844 Chromium lab sample on 23 September, observed for 3.5 seconds after load, transferred 1.20 MB across 36 finished requests on navet.app (FCP 400 ms, LCP 432 ms), 1.53 MB across 143 on demo.navet.app (FCP 468 ms, LCP 944 ms), 1.32 MB across 34 on docs.navet.app (FCP 196 ms, LCP 208 ms), and 1.23 MB across 20 on storybook.navet.app (FCP 416 ms; no LCP candidate). A second live demo trace attributed 1.28 MB to 127 scripts, led by its 389 KB entry and 149 KB icon registry. The feature branch's local demo loaded 95 scripts totaling about 883 KB of gzip files over the same window before the Calendar split. Hosting, compression, cache, and network differ, so these single-run figures are diagnostic leads, not comparable field Core Web Vitals.

A targeted source search of the app and public entrypoints found no production use of `dangerouslySetInnerHTML`, `eval`, or `new Function`. The three production `window.open` call sites include `noopener,noreferrer`, and the shared `Link` primitive supplies `noreferrer` for new tabs. This is a sink review, not a full security assessment.

## Tagged findings — no flow change yet

| Tag | Evidence | Next safe step |
| --- | --- | --- |
| `BUNDLE-DEMO-INITIAL` | After section splitting, CSS source scoping, direct dashboard imports, deferred Add Card, vacuum, Weather, Calendar, Energy Now, and Switch loading, and Security wrapper splitting, the demo still emits an 814,184-byte entry script and a 549 KiB stylesheet. A trial to defer the lower Alarm card increased the entry from 860,668 to 871,096 bytes and added an 18,888-byte chunk. Person and Cover trials added transfer or loaded their new chunk during initial Home, so those trials were reverted. A MediaCard split reduced the entry from 814,220 to 691,085 bytes, but its 161.7 KB chunk still loaded during initial Home: the speaker card starts at 671 px in an 844 px phone viewport. That trial would replace a visible card with a loading control, so it was reverted. A source map shows sizable preview runtime, RoomNav, climate, media, and light card sources in the remaining entry. | Profile shared dependency paths before another split. Measure cold requests, first-viewport content, and keyboard access as well as chunk size; shared dependencies and proximity to the viewport can erase an apparent entry-size gain. |
| `BUNDLE-SHARED-STYLESHEET` | The standalone build still sends one 549,322-byte (60,236-byte gzip) initial stylesheet. Its Tailwind source scans shared components and every app feature, including sections whose JavaScript loads later. | Prototype route-level utility CSS with explicit ownership, then compare total CSS, first-load transfer, and rendered cards, Settings, dialogs, and all four themes. Removing feature sources directly could leave lazy sections unstyled. |
| `BUNDLE-ICON-REGISTRY` | `packages/app/src/constants/icon-map.ts` imports the full Lucide namespace to resolve arbitrary stored icon names synchronously. The current demo chunk is 622,753 bytes (153,252 gzip); the website chunk is 662,234 bytes (163,998 gzip). Light, switch, sensor, and chore cards call the resolver synchronously, as do room symbols and icon pickers, so replacing it with Lucide's asynchronous `DynamicIcon` would change the first render of saved custom icons and the picker validation contract. The demo preloads the registry for Home. Lucide's [dynamic icon guidance](https://lucide.dev/guide/react/advanced/dynamic-icon-component) also warns that a dynamic resolver can add requests and visual flashes. | Preserve all persisted names and the synchronous card display while prototyping a separate manifest and preloading strategy. Measure cold requests, first card paint, custom-icon fidelity, and total emitted assets before switching; a smaller eager chunk alone would not prove faster dashboard startup. |
| `BUNDLE-ADD-CARD-DIALOG` | A trial that lazy-loaded the dashboard Add Card dialog made eager JavaScript 0.1 KiB larger and changed authenticated transition JavaScript by only 0.1 KiB. Other static imports still retain its dependencies. The trial was reverted. | Trace the shared import graph before changing this dialog boundary; keep its immediate open behavior unless a measured split removes meaningful startup work. |
| `BUNDLE-DASHBOARD-BARREL-IMPORTS` | Replacing dashboard barrel imports in the app's entity grid, Security camera dashboard, Settings controller, and Home Assistant panel with direct imports increased standalone eager JavaScript from 752.1 to 758.2 KB and authenticated transition JavaScript from 140.2 to 146.1 KB. The trial was reverted. | Keep the existing import boundary until the chunk graph explains the extra 6 KB of eager and transition code; do not apply the demo's direct-import result blindly to the authenticated app. |
| `BUNDLE-STANDALONE-DEMO` | `apps/standalone/vite.config.ts` enables the embedded `/demo` route by default, so the standalone artifact includes demo-only assets such as both camera sample formats. | Decide whether the embedded route is an installation contract before changing the default. If retained, assess a separate optional distribution. |
| `BUNDLE-EAGER-PROVIDER-STORE` | The standalone entry statically imports a 251,069-byte `integration-store` chunk. Its production sourcemap includes 106 sources, among them Home Assistant, Homey, and openHAB adapters, mapping, auth, and connection code. | Preserve current provider startup behavior while tracing when each runtime is registered and first needed; split an adapter only after an authenticated connection test proves no delayed or missing commands. |
| `BUNDLE-DOCS-INLINE-SCRIPTS` | The 79 built docs HTML files contain 1,006 inline script instances totaling 688,695 uncompressed bytes, but only 13 distinct scripts totaling 8,902 bytes. A trial externalized seven repeated module scripts and removed 457,649 raw HTML bytes, yet added seven cold-page requests and about 1.3 KiB more compressed transfer on a representative page. The trial was reverted. | Keep the current inline modules for cold-page speed. Reconsider a shared bundle only if it can reduce both requests and first-load transfer while preserving theme setup, search, navigation, and script order. The CSP hashes preserve the current loading behavior. |
| `BUNDLE-DOCS-SCREENSHOT-ALIASES` | Six groups of how-to screenshots have identical bytes under different topic-specific URLs, adding 674,684 bytes to the docs output. The largest group reuses one 50,604-byte dashboard image at 11 paths. Built guides reference those paths, so deleting the copies would break images and possibly direct links. Some captions describe a specific result that the shared generic image does not show. | Replace aliases with accurate, task-specific screenshots before removing old URLs, or introduce verified redirects for any URLs that must remain reachable. Do not collapse the paths merely to reduce bytes while the captions and external links still depend on them. |
| `A11Y-GRADIENT-CONTRAST` | A loaded 390 px axe scan found no confirmed WCAG A/AA violations across the eight demo sections, but color contrast remained inconclusive for composited surfaces: 90 Home nodes, 66 Energy nodes, 59 Security nodes, 51 Household nodes, 18 Climate nodes, 6 Lights nodes, and 64 Media nodes. | Check representative text against rendered gradient and translucent backgrounds in each theme before changing shared colors; an automated incomplete result does not establish a pass or failure. |
| `SECURITY-CUSTOM-IFRAME` | Advanced sidebar actions accept user-configured HTTP(S) URLs and render them in an iframe without a `sandbox` attribute. URL sanitization blocks non-HTTP schemes, but does not restrict frame capabilities. The [HTML standard](https://html.spec.whatwg.org/dev/iframe-embed-object.html) defines sandbox tokens that can restrict scripts, forms, origin access, and navigation. | Test representative user extensions, including sign-in and same-origin pages, before setting a sandbox policy. Applying a restrictive policy without that compatibility check could break embedded workflows. |

## Validation performed

- `pnpm build`, `pnpm build:demo`, `pnpm build:website`, `pnpm docs:build`, `pnpm build:storybook`, and `pnpm typecheck` passed.
- Focused Biome, `pnpm check:bundle-budget`, `pnpm check:website-audit`, `pnpm check:stories`, provider and UI-kit boundary checks, runtime policy and lockfile checks, and `git diff --check` passed.
- `pnpm check:docker` passed its standalone and Home Assistant add-on runtime checks.
- `pnpm audit --prod --audit-level high` reported no known production dependency vulnerabilities when rerun with network access on 23 September.
- The broad Tier 3 test run found one outdated hero assertion from the earlier navigation change. The legacy test was classified **Rewrite** and now verifies that Explore the demo is a link to the configured demo URL. Its focused run passed, then the full Tier 3 rerun passed: 522 files and 3,560 tests.
- The media-query hook, dashboard layout, and app unit checks passed together (43 tests). A later 390 px demo scan covered all eight routes, the mobile Settings list and detail, and keyboard scrolling in Energy and Climate; each route had one main landmark and no horizontal page overflow.
- Local browser checks covered website skip-link focus and hero dimensions, plus all eight demo section landmarks and demo skip-link focus.
- A generated-HTML check found one main landmark, one h1, and an English document language on all 79 docs HTML pages. Storybook's shell and preview frames are JavaScript-driven and were excluded from this static landmark count.
