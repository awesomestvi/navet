# RSS TLS test fixtures

`cert.pem` and `key.pem` are a public, self-signed test pair for `feed.example`. They contain no
production credentials and exist only to exercise the RSS transport's HTTPS validation behavior.
